VISION.md defines a new color system (lines 343-358):

Current primary #378ADD (blue) → New primary #4A4558 (obsidian-orchid)

New CSS variables:
--primary: #4A4558
--primary-dark: #3d3852
--canvas-bg: #F9F9FB (was #F8F7F4)
--border-default: #E2E8F0 (was #D3D1C7)
--border-selected: #4A4558
--text-primary: #1E293B (was #2C2C2A)
--text-secondary: #475569 (was #888780)
--text-muted: #94A3B8 (was #B4B2A9)
--success-green: #10B981 (was #639922)
--error-red: #9E3F4E (was #E24B4A)
--warning-amber: #F59E0B (was #BA7517)

Typography (VISION lines 328-336):
- Add Google Fonts: Manrope (700,800) + Inter (400-700) + JetBrains Mono
- Logo: Manrope 800
- Node names: Inter 700 uppercase
- Body text: Inter
- Code: JetBrains Mono

Canvas background: #F9F9FB + radial-gradient dot grid (#ACB3B8 1px, 24px spacing)

Update: globals.css, index.html (Google Fonts link), all components using old color vars

## Agent Review Feedback — Address These Issues:
1. Remove frontend/package-lock.json from this PR — it's unrelated to the theme change and adds 5K+ lines of noise. If it's needed, it belongs in a separate commit/PR. 2. Extract the hardcoded rgba values in NodeCard.tsx error/success backgrounds into CSS variables or at minimum add a comment linking them to the design tokens. 3. Define the dot-grid color (#ACB3B8) as a CSS variable rather than hardcoding it in Canvas.tsx. 4. The .gitignore additions (.claude-hub-run.sh, .claude-hub-task.md) are unrelated to this ticket and should be in a separate commit.

You are on branch feature/update-theme-primary-color-4a4558-new-cs-6289c5 which already has your previous work.
Read the existing code, address the review feedback above, commit, push, and update the PR.
Do NOT create a new PR — push to the same branch so the existing PR #92 is updated.

## IMPORTANT — Polish Pass (do this BEFORE final verification)
Before marking your work as done, do one round of self-review:
1. Re-read the ticket description and VISION.md spec for the area you changed.
2. Compare what you built against the spec — did you miss anything?
3. Check for edge cases, error handling, and UI/UX quality.
4. Run the feature end-to-end if possible. Fix anything that feels rough.
5. Review your own code — would you approve this PR? Clean up anything you wouldn't.

## IMPORTANT — Final Verification (do this LAST before exiting)
Before you finish:
1. If you modified frontend files, run `cd frontend && pnpm exec tsc -b` to check for TypeScript errors. Fix any errors before pushing.
2. Run `git status` and `git log --oneline -5` to verify:
   - No uncommitted changes remain (working tree clean)
   - All commits have been pushed to the remote branch
   - A PR exists (create one if missing)
If anything is missing, fix it before exiting.